# apiVersion: 1

# datasources:
#   - name: Prometheus
#     type: prometheus
#     access: proxy
#     orgId: 1
#     url: http://prometheus:9090
#     basicAuth: false
#     isDefault: true
#     editable: true
datasources:
  - name: 'Prometheus'
    type: 'prometheus'
    id: 'prometheus'
    access: 'server'
    org_id: 1
    url: 'http://prometheus:9090'
    is_default: true
    version: 1
    editable: true
